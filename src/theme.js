import spacing from './spacing';

export default {
  spacing,
  mediaQueries: {
    tablet: '(min-width: 768px)',
    darkMode: '(prefers-color-scheme: dark)',
  },
  shape: {
    borderRadius: '4px',
  },
};
